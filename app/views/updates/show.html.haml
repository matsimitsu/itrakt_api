%h1 Update

%dl
  %dt Time
  %dd= @update.created_at.strftime("%d-%m-%Y %I:%M")

  %dt Timestamp
  %dd= link_to @update.timestamp, "http://www.thetvdb.com/api/Updates.php?time=#{@update.timestamp}&type=all"

  %dt Shows
  %dd= @update.series.length

  %dt Episodes
  %dd= @update.episodes.length

  %dt Status
  %dd= @update.status

%table
  %tr
    %th Name
    %th type
    %th Status
    %th Message
  - @update.results.each do |key, result|
    %tr
      %td
        - if result['show_id'].present?
          = Show.find(result['show_id']).name
        - elsif result['episode_id'].present?
          = Episode.find(result['episode_id']).name
        - else
          TVDB ID:
          = key
      %td= result['type']
      %td= result['status']
      %td= result['message']
